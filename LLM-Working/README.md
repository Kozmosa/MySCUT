# Historical working archive

此目录不再保存当前权威文档。`archive/2026/` 中的日志、Impl、计划和审计仅用于理解历史背景，内容不再维护，可能与当前代码和流程冲突。

仓库历史脱敏会使归档中记录的旧 commit hash 全部失效。请使用 `docs/`、根目录公共文档、当前代码和新的 Git 历史作为事实来源。

普通实现由 commit 和 Pull Request 记录；只有长期架构决策进入 `docs/adr/`。
