> 历史归档：本文不再维护。仓库历史脱敏后，文中记录的旧提交哈希均已失效。

# 2026-02-27 Work Summary (to now)

## 时间范围

- 统计区间：`2026-02-27 00:00` 至当前（含 2026-02-28 凌晨发版相关提交）。

## 一、核心功能交付

- **Android / Capacitor 接入**：完成 WebView 壳接入、硬件返回键处理、状态栏风格同步。
  - 代表提交：`1a0ae97`、`d86e2cc`
- **手册模块上线（底栏平级）**：新增“手册”Tab 与 `/manual` 视图，形成与课程/我的并列的主导航结构。
  - 代表提交：`8d2d0e2`、`1052aee`
- **手册内容集成（docs 子站）**：
  - 接入 `survive-in-scut` 子模块
  - 建立主站+文档联合构建链（`build:full`）
  - Android 打包默认包含 docs 资源
  - 代表提交：`a8782dd`、`b705c1a`、`69aaff7`
- **手册体验迭代**：
  - 从外链跳转升级为 `/manual` 内嵌显示
  - 修复 Android 下 `/docs/` 回退到 SPA 的空白问题（改为 `/docs/index.html`）
  - 修复顶部安全区遮挡与底栏覆盖策略（最终改为 iframe 不被底栏遮挡）
  - 代表提交：`8a9cd94`、`0a2d480`、`36e096d`
- **手册来源策略**：支持在线优先、本地回退。
  - 新增“启用本地手册”开关（全局设置）
  - 远程加载失败/超时自动切本地并提示
  - 代表提交：`bf04d5e`、`c394e7b`

## 二、课表能力升级

- **导出能力**：支持从课表设置导出 `.wakeup_schedule` 与 `.qms`。
  - 代表提交：`27ad6c1`、`4d4778e`
- **导入能力整合**：将导入入口统一为“导入课表”，整合 WakeUp / 教务 HTML / QMS / 教务 PDF（占位页）。
  - 代表提交：`a251341`、`d71b33f`、`91b8703`
- **按钮结构重排**：导入/导出/切换合并为单组，位于“当前课表”上方。
  - 代表提交：`cc714f8`
- **课表渲染增强**：
  - 支持跨节课程块（rowSpan）
  - 加入周数据预载缓存（前后 3 周）
  - 课程块展示课程名 + 教室 + 教师三行文案
  - 代表提交：`7cc3b28`
- **课表管理增强**：
  - 切换列表支持删除课表（名称二次确认）
  - 新增“自动精简提示信息”开关（默认开启）
  - 教室前缀精简规则支持：大学城/五山/广州国际校区
  - 代表提交：`5bf59dd`

## 三、主题系统升级

- **全局主题从“单模式”升级为“套装 + 模式”二维模型**：
  - 套装：`default`、`palacePlum`（占位）
  - 模式：亮色 / 暗色 / 跟随系统
- 主题控制入口从“我的”主页迁移到“全局设置”，并与手册开关同页管理。
- 代表提交：`f050241`、`8914512`、`2e5e5d0`

## 四、更多页与参与入口

- 新增“关于”信息卡片（作者、编辑部成员、免责声明、鸣谢）。
- 新增“用户协议与隐私声明”弹窗与“开源许可证”清单弹窗。
- 新增“查看TODO，参加贡献！”弹窗，分区展示 `APP` 与 `手册` TODO。
- TODO 数据改为构建期快照生成并纳入仓库，build 时自动覆盖更新。
- 代表提交：`9f6325b`、`795abc6`、`7e601c9`、`244f6a0`

## 五、构建 / 发版流水线演进

- **构建期子模块策略**：
  - 所有 build 前拉取手册子模块最新内容
  - 日常 build 结束后恢复到 pinned 提交，避免非发版场景更新 submodule 指针
  - `build:full` / `build:android` 包含 `cap sync android`
  - 发版场景新增专用准备脚本更新 submodule 指针
  - 代表提交：`e665237`
- **Release 自动化**：
  - 增加 tag 触发的 GitHub Release workflow
  - release 脚本支持自动化版本更新、构建、打 tag、推送
  - 发布资产上传由 workflow 接管（脚本仅准备并提交资产）
  - 增加“发版时同步 assets 到手册 root-assets，并在子模块提交推送”环节
  - 代表提交：`57967f3`、`b3e66fc`、`9de46a7`、`fb76ccd`

## 六、版本里程碑

- `v0.2.0`：首轮完整集成（手册入口、主题升级、导入导出能力初步完善）
  - 代表提交：`521a1a0`
- `v0.2.3`：继续发版验证（workflow 发布链路）
  - 代表提交：`b09b39e`
- `v0.2.5`：发版准备提交（自动化流程继续收敛）
  - 代表提交：`c6d061d`

## 七、文档产出

- 新增：`LLM-Working/ManualDocsImpl0227.md`
- 新增：`LLM-Working/QMS_Schema.md`
- 新增：`LLM-Working/GlobalThemeImpl0227.md`
- 新增：`LLM-Working/ReleaseImpl0228.md`
- 更新：`LLM-Working/20260226.md`

## 当前状态

- 主导航三栏（课程/手册/我的）稳定可用。
- 手册支持在线优先 + 本地回退，构建链可自动打包 docs 到 Android 资源。
- 课表导入导出、主题套装、课表删除与提示信息精简开关均已可用。
- 发版链路已切换为“本地准备资产 + workflow 创建 release 并上传附件”。
