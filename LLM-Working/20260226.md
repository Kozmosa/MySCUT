# 2026-02-26 Progress Log

## Completed

- 完成首页骨架到多页面路由改造：底栏双 Tab（课程/我的）、`/courses` 与 `/mine` 路由切换。
- 完成“我的”页基础功能：
  - 头像上传（JPG/PNG）
  - 顶部裁剪为最大内接正方形
  - Base64 持久化到 `localStorage`
  - 刷新后自动回显。
- 完成“课表设置”页：
  - 学期起始时间设置（Modal + DatePicker）
  - 导入 WakeUp 课表文件（`*.wakeup_schedule`）
  - 导入结果持久化到 `localStorage`。
- 设计并落地课程表核心数据结构与解析流程：
  - 新增 `src/core/schedule/` 领域模块（types/import/storage/selectors/theme）
  - 支持 WakeUp 五行 JSON 无损导入（保留 raw 结构）
  - 支持按周渲染与冲突课程 `+N` 展示。
- 完成课程页交互增强：
  - 左右滑动切周（跟手平移 + 回弹 + 周数下限）
  - 顶栏左右箭头按钮切周
  - 点击课程块弹出课程详情模态框（课程名/教室/教师/周次/节次）。
- 完成视觉优化：
  - 顶栏与底栏毛玻璃效果
  - 顶栏粘性固定
  - 空白单元格不渲染占位块。
- 完成课表主题系统：
  - 主题配置解耦到独立文件
  - 新增主题持久化
  - 扩展多套预设主题与三档文字色配置
  - `skyBlue` 更名为“默认”
  - 新增 `preset` 模式（统一主题着色）与 `wakeup` 模式（沿用原始课程色）。
- 修复关键问题：
  - `touchmove` passive listener 报错（移除 `preventDefault`，使用 `touch-action: pan-y`）
  - 上下滚动提示与顶栏/底栏遮挡计算问题
  - 提示仅在遮挡区域存在课程块时显示（空白遮挡不提示）。
- 固化工程约定：
  - 完善 commit message 规范（Conventional Commit + 中文 Markdown 无序列表正文）
  - 新增“保存实现文档”技能约定与模板。

## Documentation Added / Updated

- 新增：`LLM-Working/DATA_STRUCTURE.md`
- 新增：`LLM-Working/ScheduleImpl0226.md`
- 新增：`LLM-Working/ThemeImpl0226.md`
- 新增：`LLM-Working/SKILL_保存实现文档.md`
- 新增：`LLM-Working/Impl_TEMPLATE.md`
- 更新：`PROJECT_BASIS.md`（提交规范 + 保存实现文档技能约定）
- 更新：`AGENTS.md`（同步文档技能执行约定）。

## Key Commits (Today)

- `cb08877` feat: enhance mine and courses pages with detailed views and navigation
- `30aac59` feat: add avatar persistence and schedule settings
- `4f63870` chore: ignore tsbuildinfo and document commit conventions
- `c7c077a` feat: add swipe week navigation for schedule
- `adbb59e` feat: import and persist wakeup schedule data
- `a03678b` chore: add commit message rule and wakeup sample file
- `b9b74df` feat: add schedule theme presets and doc skill workflow
- `00e1b35` feat: support preset theme rendering and text colors
- `cf2e047` feat: add course detail modal and fix swipe warning
- `b18c31a` feat: add contextual scroll hints for schedule viewport

## Current State

- 课程页已具备：导入数据渲染、滑动/按钮切周、课程详情、主题切换、上下遮挡提示。
- 设置页已具备：学期起始时间、课表导入、配色设置（含“自定义”占位）。
- 本地构建可通过（`npm run build`）。

## Next Steps

1. 课程详情补充时间段展示（基于 `timeSlots` 映射开始/结束时间）。
2. 主题弹窗增加选中态与实时预览，提升可感知性。
3. 自定义主题能力落地（替换当前占位按钮）。
4. 将课程表数据与主题改为可热更新读取（当前为页面初始化读取）。
