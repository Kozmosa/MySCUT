## v0.4.0

本版本聚焦课表导入导出可靠性、QMS 体积优化与移动端体验修复。

### 新增与增强

- 升级 QMS 导出到 v2：
  - 移除 `raw` 冗余字段
  - 非 builtIn 预设仅写入预设标识符
  - builtIn 预设保留完整 `timeSlots`
- 导出界面新增“自定义写入的时间表”能力，可控制导出到 WakeUp/QMS/压缩QMS 的时间表写入策略
- 华工教务 HTML 导入增强：
  - 识别并导入课程学分
  - 保存每节课清洗后的详细文本（备用详情字段）
- 课程详情弹窗新增学分显示（`credit <= 0` 显示为 `-`）

### 修复

- 修复 WakeUp 导入节次跨度解析与校园预设自动匹配问题
- 修复 SCUT HTML 单双周解析问题
- 导入链路增加时间表冗余裁剪（命中相邻 `endTime` 差值 10 分钟后裁剪后续冗余节点）
- 修复 iOS 中“设置学期起始时间”打开 DatePicker 时页面自动缩放导致横向滚动异常的问题
- 修复华工教务导入后主题继承逻辑，按当前主题进行上色

### 导入体验改进

- QMS 文件选择器支持 `.txt`
- HTML 导入文件选择器支持 `.mht` / `.mhtml`
- 统一设置页与交集页相关导入后缀支持

### 工程与文档

- 更新/补充 QMS v2 相关结构与导入策略文档
- 忽略本地导入导出测试产物（`LLM-Working` 下的 qms/json/html/htm/mhtml）
